---
title: Основной синтаксис MDX (Markdown + JSX)
date: '2025-10-31'
author: 'Лев Николаевич'
tags: ['default']
postLayout: 'column'
draft: false
summary: Подробное руководство по основным элементам форматирования MDX, включая Markdown, JSX и их совместное использование.
cover: '@/assets/og-image.png'
---

# 📝 Синтаксис MDX: Markdown + JSX

MDX — это суперсет **Markdown**, который позволяет встраивать **JSX** (как в React/Astro) прямо в ваши файлы контента.

---

## 1. Блок метаданных (Frontmatter)

Этот блок использует синтаксис **YAML** и обязательно должен находиться в самом начале файла, заключенный между тройными дефисами (`---`).

```yaml
---
title: Заголовок поста
date: '2025-10-31'
draft: false
tags: [mdx, синтаксис, руководство]
# Любые другие метаданные для вашего шаблона (layout)
---

Комментарии
В MDX для комментирования JSX-элементов и Markdown-текста используются фигурные скобки с синтаксисом JavaScript.

{/* Это самый распространенный и безопасный JSX-комментарий. */}

<p>
    Текст до.
    {/* Комментарии также можно использовать внутри JSX-элементов. */}
    Текст после.
</p>

{/* Стандартные HTML-комментарии () также работают, 
    но могут быть видны в итоговом HTML, если не удалены сборщиком. */}

import MyComponent from './Component.astro';
// Обычные JS-комментарии допускаются перед импортами или экспортами.

Базовый Markdown
Заголовки (Headings)
Используйте символ # для создания иерархии заголовков.

# H1: Главный заголовок
## H2: Раздел
### H3: Подраздел

Выделение текста (Emphasis)

Синтаксис
Результат*наклонный*наклонный**жирный**жирный***жирный и наклонный***жирный и наклонный~~зачёркнутый~~~~зачёркнутый~~



Ссылки и Изображения

[Текст ссылки](https://example.com/ "Всплывающая подсказка")

![Альтернативный текст изображения](/assets/image.jpg)

Списки (Lists)

* Неупорядоченный (Bullet 1)
* Неупорядоченный (Bullet 2)

1. Упорядоченный (Item 1)
2. Упорядоченный (Item 2)
   1. Вложенный пункт

   Цитаты (Blockquotes)
Используйте символ >. Цитаты могут быть многострочными.

> Это однострочная цитата.
>
> Это многострочная цитата, которая может содержать несколько параграфов.
>> А это вложенная цитата.

Горизонтальная линия и Таблицы

---
Используйте три или более дефиса, звездочки или подчеркивания.
***

| Заголовок 1 | Заголовок 2 |
| :--- | ---: |
| Левое выравнивание | Правое выравнивание |
| Данные A | Данные B |

Код и JSX-элементы
Встроенный и Блочный код

Для выделения встроенного кода используйте одинарные обратные кавычки: `const x = 5;`.

Для блоков кода используйте тройные обратные кавычки и укажите язык:
```html
<header>
    <h1>Заголовок</h1>
</header>

### JSX-Элементы и Компоненты

Вы можете вставлять любой валидный JSX/HTML. Главное правило: **самозакрывающиеся теги** (как `<source>`, `<br>`, `<img>`) должны заканчиваться слешем `/`.

```mdx
import VideoPlayer from '../components/VideoPlayer.jsx';

{/* 1. Использование компонента с пропсами.
    2. Фигурные скобки {} для JS-выражений (передача числа, переменной).
*/}
<VideoPlayer videoId={12345} size="large" />

{/* 3. Стандартный HTML, следующий правилам JSX.
*/}
<audio controls>
    <source src="/audio/track.mp3" type="audio/mpeg" />
    Ваш браузер не поддерживает аудио.
</audio>
